/**
 * Workspace Switcher Page — THIN PAGE.
 */

import { WorkspaceSwitcherFeature } from '@/features/workspace-switcher';

export default function WorkspacesPage() {
  return <WorkspaceSwitcherFeature />;
}
